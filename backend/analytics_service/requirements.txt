# TANAW Analytics Service - Hybrid Column Mapping & Analytics System
# Requirements for all 10 phases of the implementation

# ===== CORE DEPENDENCIES (Required) =====
# These are essential for TANAW to function

# Data processing and analytics
pandas>=2.0.0
numpy>=1.24.0

# Web framework (if using Flask API)
flask>=3.0.0
flask-cors>=4.0.0

# Date handling
python-dateutil>=2.8.0

# ===== OPTIONAL DEPENDENCIES (Enhanced Features) =====
# TANAW will work without these but with reduced capabilities

# For enhanced fuzzy matching (Phase 3: Local Analyzer)
# Fallback: Uses difflib if not available
rapidfuzz>=3.0.0

# For OpenAI GPT integration (Phase 5: GPT Escalation)
# Fallback: Uses mock responses if not available
openai>=1.0.0

# For production MongoDB knowledge base (Phase 7: Knowledge Base)
# Fallback: Uses SQLite if not available
pymongo>=4.0.0

# For system monitoring (Phase 10: Testing & Monitoring)
# Optional: Only needed for advanced monitoring
psutil>=5.9.0

# ===== DEVELOPMENT DEPENDENCIES (Testing & Development Only) =====
# These are only needed for development and testing

pytest>=7.0.0
pytest-cov>=4.0.0

# ===== INSTALLATION NOTES =====
#
# BASIC INSTALLATION (Core functionality only):
#   pip install pandas numpy flask flask-cors python-dateutil
#
# RECOMMENDED INSTALLATION (Core + enhanced features):
#   pip install -r requirements.txt
#
# PRODUCTION INSTALLATION (Core + production features):
#   pip install pandas numpy flask flask-cors python-dateutil rapidfuzz openai pymongo
#
# DEVELOPMENT INSTALLATION (Everything including testing):
#   pip install -r requirements.txt
#   pip install pytest pytest-cov
#
# ===== COMPATIBILITY =====
# Python Version: 3.8 or higher recommended
# Tested on: Python 3.11, 3.12, 3.13
# OS: Windows, Linux, macOS

